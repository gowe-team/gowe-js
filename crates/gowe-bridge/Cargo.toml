[package]
name = "gowe-bridge"
version = "0.1.0"
edition = "2021"

[dependencies]
base64 = "0.22"
gowe = { path = "../../../gowe-rust" }
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
